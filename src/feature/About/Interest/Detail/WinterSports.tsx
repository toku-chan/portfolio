import { Heading } from '~/designSystems';
import { DetailWrapper } from '../../styles';

export function WinterSports() {
  // MEMO: ブログ作ったらそっちに誘導するとかでも良いかも
  return (
    <DetailWrapper>
      <Heading.sectionTitle>スノボー</Heading.sectionTitle>
      <p>毎シーズン滑りに行ってます。</p>
    </DetailWrapper>
  );
}
